[project]
name = "aws-serverless-patterns-workshop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aws-cdk-lib>=2.260.0",
    "aws-lambda-powertools>=3.29.0",
    "boto3>=1.43.34",
]
